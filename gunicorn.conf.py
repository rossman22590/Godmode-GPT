workers = 1
threads = 50
bind = "0.0.0.0:8080"
accesslog = "-"  # Log access logs to stdout
errorlog = "-"   # Log error logs to stdout
loglevel = "info"  # Choose an appropriate log level: debug, info, warning, error, or critical
